# Scratchpad

## Current Task: Fix Invoice Numerical Sorting (1000+ invoices)
[X] Identified issue: invoice_number sorted as string, not numerically
[X] Fixed default_order_by() in Mdl_invoices.php - added LENGTH() for natural numeric sorting

## Previous Task: Batch Payment Feature for Client Invoices
[X] Add batch payment button to client invoice view
[X] Create batch payment modal view
[X] Add AJAX endpoint for batch payment modal
[X] Add AJAX endpoint for processing batch payments
[X] Add JavaScript for batch payment functionality
[X] Add language translations

### Implementation Details:
**Batch Payment Feature:**
- Added "Batch Payment" button to Client > Invoices view
- Button appears when invoices are selected (alongside PDF/Bill.com buttons)
- Opens a modal showing all selected invoices with their balances
- Automatically pays the FULL balance of each selected invoice
- Allows entering:
  - Payment date
  - Payment method
  - Note (added to all payment records)
- Displays total payment amount (sum of all invoice balances)
- Creates individual payment records for each selected invoice
- Automatically refreshes page after successful batch payment
- Filters out already-paid invoices (zero balance)
- Shows success/error counts and details

### Files Created/Modified:
1. `application/modules/payments/views/modal_add_batch_payment.php`:
   - New modal view for batch payment entry
   - Shows list of selected invoices with balances
   - Form for payment details
   - JavaScript to handle form submission via AJAX

2. `application/modules/payments/controllers/Ajax.php`:
   - Added `modal_add_batch_payment()` method to load modal
   - Added `add_batch()` method to process batch payments
   - Validates each invoice and amount
   - Creates individual payment records
   - Returns success/error counts

3. `application/modules/clients/views/view.php`:
   - Added batch payment button to header
   - Updated toggleClientBatchButton() to show/hide payment button
   - Added JavaScript handler to load batch payment modal

4. `application/language/english/ip_lang.php`:
   - Added translations for batch payment feature

## Previous Task: Infinite Scroll for Invoices and Payments Pages
[X] Create AJAX endpoint for loading more invoices
[X] Update main invoices view to add infinite scroll UI
[X] Add JavaScript for infinite scroll functionality
[X] Hide pagination arrows for infinite scroll
[X] Add AJAX endpoint for client payments infinite scroll
[X] Add infinite scroll UI to client payments tab
[X] Add AJAX endpoint for main payments page
[X] Add infinite scroll UI to main payments page
[X] Add language translations
[X] Test infinite scroll with filters and search

### Implementation Details:
**Main Invoices Screen Infinite Scroll:**
- Replaced traditional pagination with infinite scroll for smoother browsing
- Added AJAX endpoint `load_more_invoices()` in Invoices controller
- Automatic loading when user scrolls within 200px of bottom
- Manual "Load More" button as fallback
- Shows loading spinner and "No more invoices" message at end
- Loads 20 invoices per request
- Works seamlessly with existing status filters (All, Draft, Sent, Viewed, Paid, Overdue)
- Works seamlessly with search functionality
- Maintains checkbox selections and batch operations across dynamically loaded content
- Uses proper CSRF token handling for AJAX requests
- Renders complete invoice rows with all options (edit, PDF, email, payment, Bill.com, delete)
- Preserves Bill.com status indicators on dynamically loaded invoices
- Hidden pagination controls (still exist for compatibility but not visible)

**Client > Payments Tab Infinite Scroll:**
- Added AJAX endpoint `load_more_payments()` in Clients/Ajax controller
- Automatic loading when user scrolls within 500px of bottom
- Manual "Load More" button as fallback
- Shows loading spinner and "No more payments" message at end
- Loads 20 payments per request
- Renders complete payment rows with all options (edit, delete)
- Hidden pagination controls for payments tab

**Main Payments Screen Infinite Scroll:**
- Added AJAX endpoint `load_more_payments()` in Payments controller
- Automatic loading when user scrolls within 200px of bottom
- Manual "Load More" button as fallback
- Shows loading spinner and "No more payments" message at end
- Loads 20 payments per request
- Renders complete payment rows with all options (edit, delete)
- Hidden pagination controls

### Files Modified:
1. `application/modules/invoices/controllers/Invoices.php`:
   - Added `load_more_invoices()` AJAX endpoint method
   - Accepts offset, limit, status, and search parameters
   - Applies same filters as main status() method
   - Renders complete invoice rows as HTML
   - Returns JSON with html, count, and has_more flag
   
2. `application/modules/invoices/views/index.php`:
   - Hidden pagination controls (display:none)
   - Added infinite scroll UI elements (load-more button, loading spinner, end message)
   - Added JavaScript for infinite scroll functionality
   - Auto-load on scroll (200px from bottom)
   - Manual load button as fallback
   - Integrated with existing checkbox and batch operations

3. `application/modules/clients/controllers/Ajax.php`:
   - Added `load_more_payments()` AJAX endpoint method for client payments
   - Accepts client_id, offset, and limit parameters
   - Renders complete payment rows as HTML
   - Returns JSON with html, count, and has_more flag

4. `application/modules/clients/views/view.php`:
   - Added infinite scroll UI for payments tab
   - Hidden pagination controls for payments tab
   - Added JavaScript for payment infinite scroll
   - Auto-load on scroll (500px from bottom)
   - Manual load button as fallback

5. `application/modules/payments/controllers/Payments.php`:
   - Added `load_more_payments()` AJAX endpoint method for main payments page
   - Accepts offset and limit parameters
   - Renders complete payment rows as HTML
   - Returns JSON with html, count, and has_more flag

6. `application/modules/payments/views/index.php`:
   - Hidden pagination controls (display:none)
   - Added infinite scroll UI elements (load-more button, loading spinner, end message)
   - Added JavaScript for infinite scroll functionality
   - Auto-load on scroll (200px from bottom)
   - Manual load button as fallback
   
7. `application/language/english/ip_lang.php`:
   - Added 'error_loading_invoices' translation
   - Added 'no_more_payments' translation
   - Added 'error_loading_payments' translation

# Lessons

## User Specified Lessons

- You have a python venv in ./venv. Use it.
- Include info useful for debugging in the program output.
- Read the file before you try to edit it.
- Due to Cursor's limit, when you use `git` and `gh` and need to submit a multiline commit message, first write the message in a file, and then use `git commit -F <filename>` or similar command to commit. And then remove the file. Include "[Cursor] " in the commit message and PR title.

## Cursor learned

- For search results, ensure proper handling of different character encodings (UTF-8) for international queries
- Add debug information to stderr while keeping the main output clean in stdout for better pipeline integration
- When using seaborn styles in matplotlib, use 'seaborn-v0_8' instead of 'seaborn' as the style name due to recent seaborn version changes
- Use 'gpt-4o' as the model name for OpenAI's GPT-4 with vision capabilities
- When updating invoice status after sending to Bill.com, also call generate_invoice_number_if_applicable() and mark_sent() to ensure consistency
- CSRF tokens regenerate after AJAX calls - use Cookies.get(csrf_cookie_name) to get fresh tokens
- date_from_mysql() requires true as second parameter during AJAX POST to format dates properly
- When searching invoice items, use INNER JOIN with GROUP BY to avoid duplicate results
- When searching invoices, remember that trailer numbers are stored in ip_invoice_custom table, not invoice items
- For infinite scroll on main pages, hide pagination with display:none instead of removing it entirely (maintains compatibility)
- AJAX-loaded invoice rows must include all functionality: checkboxes, options dropdown, Bill.com status indicators, etc.
- Use $(document).on() event delegation for dynamically loaded content (checkboxes, payment forms, etc.)
- When sorting string columns that contain numbers (like invoice_number), use LENGTH(col) DESC, col DESC to achieve natural numeric ordering — pure string DESC fails once values exceed consistent digit counts (e.g., "999" > "1000" in string sort)

